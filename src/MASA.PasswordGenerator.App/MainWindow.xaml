<Window x:Class="MASA.PasswordGenerator.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:MASA.PasswordGenerator.App.ViewModels"
        xmlns:views="clr-namespace:MASA.PasswordGenerator.App.Views"
        mc:Ignorable="d"
        Title="MASA Password Generator" 
        Height="800" Width="1200"
        MinHeight="650" MinWidth="950"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource BgRootBrush}"
        Foreground="{DynamicResource TextPrimaryBrush}">

    <Window.InputBindings>
        <KeyBinding Key="G" Modifiers="Control" Command="{Binding GeneratorVM.GeneratePasswordCommand}"/>
        <KeyBinding Key="C" Modifiers="Control" Command="{Binding GeneratorVM.CopyPasswordCommand}"/>
        <KeyBinding Key="R" Modifiers="Control" Command="{Binding GeneratorVM.GeneratePasswordCommand}"/>
        <KeyBinding Key="H" Modifiers="Control" Command="{Binding NavigateToCommand}" CommandParameter="History"/>
        <KeyBinding Key="OemComma" Modifiers="Control" Command="{Binding NavigateToCommand}" CommandParameter="Settings"/>
    </Window.InputBindings>

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:DashboardViewModel}">
            <views:DashboardView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:GeneratorViewModel}">
            <views:GeneratorView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:BulkViewModel}">
            <views:BulkView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:PassphraseViewModel}">
            <views:PassphraseView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:CheckerViewModel}">
            <views:CheckerView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:HistoryViewModel}">
            <views:HistoryView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:PresetsViewModel}">
            <views:PresetsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
            <views:SettingsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:AboutViewModel}">
            <views:AboutView/>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar Navigation -->
        <Border Grid.Column="0" Background="{DynamicResource BgSidebarBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,1,0">
            <DockPanel LastChildFill="True">
                <!-- App Logo & Title -->
                <StackPanel DockPanel.Dock="Top" Margin="24,28,24,25">
                    <TextBlock Text="MASA" FontSize="26" FontWeight="ExtraBold" Foreground="{DynamicResource PrimaryBrush}"/>
                    <TextBlock Text="Password Generator" FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource TextSecondaryBrush}"/>
                </StackPanel>

                <!-- Footer Status -->
                <Border DockPanel.Dock="Bottom" Margin="16,10,16,16" Padding="12" Background="{DynamicResource BgCardHoverBrush}" CornerRadius="8">
                    <StackPanel>
                        <TextBlock Text="CSPRNG Engine: ACTIVE" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource SuccessBrush}"/>
                        <TextBlock Text="DPAPI Storage: SECURE" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,2,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Navigation Links List -->
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="14,0">
                        <RadioButton Style="{DynamicResource NavRadioButton}" Content="Dashboard" IsChecked="True" Command="{Binding NavigateToCommand}" CommandParameter="Dashboard"/>
                        <RadioButton Style="{DynamicResource NavRadioButton}" Content="Generate Password" Command="{Binding NavigateToCommand}" CommandParameter="Generator"/>
                        <RadioButton Style="{DynamicResource NavRadioButton}" Content="Bulk Generator" Command="{Binding NavigateToCommand}" CommandParameter="Bulk"/>
                        <RadioButton Style="{DynamicResource NavRadioButton}" Content="Passphrase Generator" Command="{Binding NavigateToCommand}" CommandParameter="Passphrase"/>
                        <RadioButton Style="{DynamicResource NavRadioButton}" Content="Password Checker" Command="{Binding NavigateToCommand}" CommandParameter="Checker"/>
                        <RadioButton Style="{DynamicResource NavRadioButton}" Content="History" Command="{Binding NavigateToCommand}" CommandParameter="History"/>
                        <RadioButton Style="{DynamicResource NavRadioButton}" Content="Presets &amp; Policies" Command="{Binding NavigateToCommand}" CommandParameter="Presets"/>
                        
                        <Separator Background="{DynamicResource BorderBrush}" Margin="10,12"/>

                        <RadioButton Style="{DynamicResource NavRadioButton}" Content="Settings" Command="{Binding NavigateToCommand}" CommandParameter="Settings"/>
                        <RadioButton Style="{DynamicResource NavRadioButton}" Content="About" Command="{Binding NavigateToCommand}" CommandParameter="About"/>
                    </StackPanel>
                </ScrollViewer>
            </DockPanel>
        </Border>

        <!-- Main Content Area -->
        <Grid Grid.Column="1" Background="{DynamicResource BgRootBrush}">
            <ContentControl Content="{Binding CurrentViewModel}"/>
        </Grid>
    </Grid>
</Window>
